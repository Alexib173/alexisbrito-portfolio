---
interface Props {
  id: string;
  class?: string;
}
const { id, class: className = '' } = Astro.props;
---

<section
  id={id}
  class={`py-16 md:py-24 scroll-mt-24 ${className}`}
>
  <div class="max-w-3xl mx-auto px-6">
    <slot />
  </div>
</section>
